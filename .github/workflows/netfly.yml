name: 'My Netfly Workflow'

on:
  release:
    types: [published]

jobs:
  deploy:
    name: 'Deploy to Netlify'
    steps:
      - uses: jsmrcaga/action-netlify-deploy@v2.0.0
        with:
          NETLIFY_AUTH_TOKEN: nfp_SR7PvWyQUw5gLZE7CpdFej3ZRXHtukKb2b60
          NETLIFY_DEPLOY_TO_PROD: true